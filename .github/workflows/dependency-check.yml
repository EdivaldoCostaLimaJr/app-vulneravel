name: Dependency Check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  dependency-check:
    name: Run OWASP Dependency Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install OWASP Dependency-Check
        run: |
          curl -L https://github.com/jeremylong/DependencyCheck/releases/latest/download/dependency-check.zip -o dependency-check.zip
          unzip dependency-check.zip -d dependency-check
          chmod +x dependency-check/bin/dependency-check.sh

      - name: Run Dependency-Check
        run: |
          mkdir -p dependency-check-report
          ./dependency-check/bin/dependency-check.sh \
            --project "app-vulneravel" \
            --scan ./ \
            --format HTML \
            --out dependency-check-report

      - name: Upload HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-html-report
          path: dependency-check-report/depende
